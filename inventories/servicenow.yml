---
plugin: servicenow.itsm.now
keyed_groups:
  - key: os
    prefix: os
  - key: used_for
    prefix: used_for
  - key: cpu_type
    prefix: cpu_type
  - key: firewall_status
    prefix: firewall_status
